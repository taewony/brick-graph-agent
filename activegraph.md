# OKF 기반 지식 그래프를 Control Plane으로 활용한 Agentic 문서 논리 검증 시스템 설계

## 1. 서론: 아키텍처는 설계되는 것이 아니라 도출되어야 한다

첨부된 이미지들은 핵심적인 원칙을 제시한다. **"아키텍처는 설계되는 것이 아니라 도출되어야 한다"** 는 명제는 에이전트 시스템 설계의 패러다임 전환을 요구한다. 즉, 개발자가 처음부터 모든 것을 설계하는 것이 아니라, 에이전트가 실제로 수행하는 행동과 추론의 궤적(traces)을 관찰하고, 그로부터 필요한 아키텍처를 도출해내는 방식이다.

이 설계안은 다음의 핵심 원칙에 기반한다:

1. **아키텍처는 설계되지 않고 도출되어야 한다**
2. **결정론적 영역과 에이전틱 영역은 분리되어야 한다**
3. **하나의 에이전트가 추론의 전 과정을 종단적으로 소유해야 한다**
4. **그래프는 단순한 조회 계층이 아닌 제어 표면(control surface)으로 취급되어야 한다**

이 원칙들을 보고서/제안서의 논리적 모순 검출 및 수정 제안이라는 구체적인 도메인에 적용하여, OKF(Open Knowledge Format) 지식 베이스와 ActiveGraph를 결합한 Agentic 시스템을 설계한다.

---

## 2. OKF KB: 도메인 지식의 그래프 기반 표현

### 2.1 OKF(Open Knowledge Format)의 이해

OKF는 Google이 제안한 AI 시대의 지식 표현을 위한 개방형 표준이다. OKF는 지식을 다음과 같은 방식으로 표현한다:

- **Markdown 파일 + YAML Frontmatter**로 구성된 디렉터리 구조
- **Markdown 링크**를 통한 개념 간 상호 연결 (전체가 하나의 그래프)
- **필수 필드 `type`** 을 통한 노드 유형 명시

OKF는 "기계가 읽을 수 있고 인간이 이해할 수 있는" 원자화된 지식 패키지를 제공하며, RAG의 맥락 노이즈 문제를 해결하는 데 적합하다.

### 2.2 보고서 논리 검증을 위한 OKF KB 구성

보고서/제안서의 논리적 구조를 OKF KB로 구성할 경우, 다음과 같은 그래프 기반 지식 모델이 형성된다:

```
[논증노드] --SUPPORTS--> [주장노드]
[주장노드] --BASED_ON--> [근거노드]
[주장노드] --CONTRADICTS--> [주장노드]
[근거노드] --CITES--> [출처노드]
[개념노드] --DEFINES--> [용어노드]
[전제노드] --ENTAILS--> [결론노드]
```

이는 첨부 이미지에서 제시된 "WHERE-WHY" 구조와 일치한다. 즉, **지리적/구조적 맥락(WHERE)과 인과적 추론(WHY)을 그래프로 연결**하여, 단순한 정보 조회가 아닌 **구조화된 조사(investigation)를 가능**하게 한다.

OKF KB의 장점은:
- **버전 관리 가능**: Git 네이티브로 관리 가능
- **에이전트 소비 최적화**: YAML frontmatter로 AI 에이전트가 직접 활용 가능
- **크로스-링크를 통한 그래프 형성**: 마크다운 링크가 곧 그래프 엣지

---

## 3. ActiveGraph: 이벤트 소싱 기반 에이전트 런타임

### 3.1 ActiveGraph 개요

ActiveGraph는 장기 실행(long-running) 에이전트를 위한 **이벤트-소싱 반응형 그래프 런타임**이다. 이는 다음과 같은 특성을 가진다:

- **공유 그래프(shared graph)** 상에서 동작: 에이전트들이 서로 대화하는 대신 공유 그래프에 반응
- **모든 행동과 변경사항을 타입화된 로그로 평탄화**: 그래프는 에이전트의 상태를 투영(project)하며, 재생(replay)이 가능
- **감사 가능성(auditability)**: 누가, 무엇을, 왜 변경했는지 추적 가능

### 3.2 ActiveGraph와 OKF KB의 통합

OKF KB가 **정적 도메인 지식의 그래프**라면, ActiveGraph는 **동적 에이전트 활동의 그래프**이다. 이 둘의 통합은 다음과 같이 이루어진다:

```
OKF KB (정적 지식 그래프) ←→ ActiveGraph (동적 실행 그래프)
     │                              │
     │  (임포트/참조)               │  (행동/추론 기록)
     ▼                              ▼
  Control Plane: 지식 기반의 제어 표면
```

GBrain이 "내구성 있는 세계 지식"을 저장한다면, ActiveGraph는 "에이전트가 이를 어떻게 사용하거나 변경을 제안하는지"를 기록하고 통제한다.

---

## 4. Control Plane으로서의 그래프: 단순 조회가 아닌 제어 표면

### 4.1 Control Plane의 개념

Agent Control Plane(ACP)은 **자율 에이전트가 운영되는 실행 환경 위에 위치하는 거버넌스 및 운영 계층**이다. 이는:

- 에이전트가 "무엇을 할 수 있는지" (권한)
- 에이전트가 "어떻게 해야 하는지" (절차)
- 에이전트의 행동이 "올바른지" (검증)

를 정의한다.

### 4.2 그래프를 Control Plane으로 활용

Context Graph는 **제안된 결정을 통제(govern)** 하며, Agent Control Plane은 **에이전트 집단을 통제**한다. 본 설계에서는 OKF KB 그래프가 이 두 역할을 모두 수행한다:

| 계층 | 역할 | OKF KB 기반 구현 |
|------|------|------------------|
| **온톨로지 계층** | 도메인 개념과 관계 정의 | OKF 노드 타입 및 관계 정의 |
| **정책 계층** | 논리 검증 규칙 및 추론 패턴 | 그래프 패턴 매칭 및 추론 규칙 |
| **증거 계층** | 주장과 근거의 연결 | CITES, BASED_ON 관계로 증거 추적 |
| **실행 계층** | 에이전트 행동의 기록과 통제 | ActiveGraph 이벤트 로그 |

이러한 구조는 "그래프가 맥락을 추가했을 뿐만 아니라, 조사 자체를 구조화했다"는 원칙을 구현한다.

---

## 5. 구체적 적용: 보고서/제안서 논리적 모순 검출 및 수정 제안

### 5.1 도메인 특화 그래프 스키마

보고서 및 제안서의 논리 검증을 위해 다음과 같은 OKF KB 스키마를 설계한다:

```yaml
# 노드 타입
- Claim: 주장 (결론, 의견, 제안)
- Premise: 전제 (가정, 사실, 근거)
- Evidence: 증거 (데이터, 인용, 사례)
- Concept: 개념 (정의, 용어)
- Document: 문서 (보고서, 제안서 전체)
- Section: 섹션 (문서의 구성 단위)

# 관계 타입
- SUPPORTS: A가 B를 지지함
- CONTRADICTS: A가 B와 모순됨
- ENTAILS: A가 B를 함의함 (논리적 귀결)
- BASED_ON: A가 B에 기반함
- CITES: A가 B를 인용함
- PART_OF: A가 B의 일부임
- PRECEDES: A가 B보다 논리적으로 선행함
```

### 5.2 논리적 모순 검출 메커니즘

ActiveGraph의 `@llm_behavior` 데코레이터를 활용하여, 그래프에 특정 패턴이 발생할 때 자동으로 반응하는 **비판자(critic) 에이전트**를 구현한다.

**검출 패턴 예시**:

1. **직접 모순(Direct Contradiction)** :
   ```
   [Claim A] --CONTRADICTS--> [Claim B]
   ```
   → 두 주장이 동시에 참일 수 없음을 보고

2. **근거 없는 주장(Unsupported Claim)** :
   ```
   [Claim A] (BASED_ON 관계 없음)
   ```
   → 근거가 없는 주장으로 식별

3. **순환 논증(Circular Reasoning)** :
   ```
   [Claim A] --BASED_ON--> [Claim B] --BASED_ON--> [Claim A]
   ```
   → 순환 의존성 탐지

4. **전제-결론 불일치(Premise-Conclusion Mismatch)** :
   ```
   [Premise P] --ENTAILS--> [Claim C] (추론 규칙 위반)
   ```
   → 전제가 결론을 논리적으로 함의하지 않음

5. **일관성 위반(Consistency Violation)** :
   ```
   [Evidence E1] --SUPPORTS--> [Claim A]
   [Evidence E2] --SUPPORTS--> [Claim B]
   [Claim A] --CONTRADICTS--> [Claim B]
   ```
   → 동일한 근거 집합이 상호 모순되는 주장을 지지

### 5.3 수정 제안 생성 파이프라인

1. **탐지(Detection)**: `critic` 에이전트가 그래프에서 논리적 모순 패턴을 탐지
2. **진단(Diagnosis)**: 모순의 원인을 추적 (어느 전제가 잘못되었는지, 어떤 근거가 부족한지)
3. **제안(Proposal)**: 다음 중 하나의 수정 방안 제안
   - 모순되는 주장 중 하나의 삭제 또는 수정
   - 누락된 근거의 추가
   - 전제의 재정의
   - 논리적 연결의 재구성
4. **검증(Verification)**: 제안된 수정이 그래프에 적용될 때, 다른 모순을 유발하지 않는지 검증

### 5.4 개발자 역할: 큐레이터(Curator)

이 시스템에서 개발자의 역할은 **큐레이터**로 전환된다:

- **그래프 스키마 정의**: 도메인에 맞는 노드/관계 타입 설계
- **추론 규칙 정의**: 어떤 패턴이 모순으로 간주될지 정의
- **에이전트 행동 감독**: 에이전트의 제안을 검토하고 승인/거부
- **지식 그래프 품질 관리**: OKF KB의 정확성과 완전성 유지

개발자는 더 이상 모든 로직을 직접 코딩하지 않고, **에이전트가 학습하고 추론할 수 있는 지식 기반(OKF KB)과 실행 환경(ActiveGraph)을 조성하는 역할**을 한다.

---

## 6. 시스템 아키텍처 전체 구성

```
┌─────────────────────────────────────────────────────────────────┐
│                      Control Plane (OKF KB Graph)              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐│
│  │  온톨로지    │  │  정책 규칙   │  │  증거/근거 그래프        ││
│  │ (Concept,   │  │ (추론 패턴,  │  │ (Evidence-CLAIM 연결)   ││
│  │  Relation)  │  │  검증 규칙)  │  │                         ││
│  └─────────────┘  └─────────────┘  └─────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   ActiveGraph Runtime                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  @llm_behavior (critic)  ←  그래프 패턴 구독           │   │
│  │  - 모순 탐지                                            │   │
│  │  - 진단 생성                                            │   │
│  │  - 수정 제안 생성                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  이벤트 로그 (모든 행동의 타입화된 추적)                │   │
│  │  - 누가, 무엇을, 왜 변경했는지 기록                     │   │
│  │  - 재생(replay) 가능                                    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      큐레이터(개발자) 인터페이스                │
│  - 그래프 스키마 관리   - 규칙 정의   - 제안 검토/승인         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 구현 로드맵

### Phase 1: OKF KB 구축
- 보고서/제안서 도메인의 온톨로지 정의
- OKF 형식으로 지식 베이스 초기 구성
- OKF Validator를 통한 형식 검증

### Phase 2: ActiveGraph 런타임 설정
- ActiveGraph gem 설치 및 Neo4j 연결 구성
- 기본 `@llm_behavior` 패턴 정의

### Phase 3: 논리 검증 에이전트 구현
- 모순 탐지 패턴 라이브러리 구축
- `critic` 에이전트 구현 (그래프 패턴 구독 기반)
- 수정 제안 생성 LLM 프롬프트 엔지니어링

### Phase 4: 큐레이터 워크플로우
- 제안 검토 및 승인 인터페이스
- 그래프 변경 이력 관리
- 지속적 학습 루프 구축

---

## 8. 기대 효과 및 결론

이 설계는 다음과 같은 효과를 기대할 수 있다:

1. **아키텍처의 유기적 진화**: 에이전트의 실제 행동 궤적에서 아키텍처가 도출되므로, 실제 사용 사례에 최적화된 시스템이 자연스럽게 형성된다.

2. **결정론적 로직과 에이전틱 추론의 분리**: 수학적/논리적 검증은 결정론적 규칙으로, 복잡한 추론은 에이전트가 담당하여 각각의 강점을 극대화한다.

3. **완전한 감사 가능성**: 모든 에이전트 행동이 ActiveGraph 이벤트 로그에 기록되어, 어떤 논리적 판단이 어떻게 내려졌는지 추적 가능하다.

4. **개발자의 큐레이터 전환**: 개발자는 더 이상 모든 예외 케이스를 코딩하지 않고, 지식 그래프의 품질과 에이전트의 행동을 조율하는 큐레이터 역할에 집중할 수 있다.

5. **확장 가능성**: 새로운 논리 검증 패턴이 발견되면, 이를 OKF KB에 규칙으로 추가하기만 하면 에이전트가 자동으로 학습하여 적용한다.

> "그래프는 맥락을 추가했을 뿐만 아니라, 조사 자체를 구조화했다."

이 원칙은 보고서/제안서의 논리 검증 분야에서 **단순한 오류 검출을 넘어, 논리적 사고 과정 자체를 구조화하고 개선하는 Agentic 시스템**의 가능성을 제시한다. OKF KB와 ActiveGraph의 결합은 이러한 비전을 실현하는 구체적인 기술적 토대를 제공한다.

---

Timestamps:
0:00 - Pharma commercial analytics and the analyst's four steps
2:33 - V1: an agent for every step
3:26 - Why the output was incoherent
4:32 - Why it failed: signals, handoffs, and missing domain
5:57 - The rebuild: watching Claude Code in an empty directory
7:01 - Deterministic signal detection before the agent
8:05 - Consolidating to a single agent
9:22 - The knowledge graph as a control plane
11:04 - Every edge a hypothesis, and the result

## 1. 서론: Understanding의 병목 해소를 위한 Brick-by-Brick 접근

AI 시대에 인간의 **이해(understanding)가 주요 병목**이 되고, 학습 자료가 폭주하는 상황에서 에이전트의 도움은 선택이 아닌 필수이다. 그러나 단순히 정보를 요약해 주는 에이전트는 진정한 학습을 대체하지 못한다. 진정한 학습은 **개념(concept) 단위의 원자적 이해**에서 시작하여, 이를 **계층적으로 조합**해 복합적인 지식 체계를 구축하는 과정이다.

이러한 맥락에서 **"brick-by-brick" 접근법**은 핵심적인 통찰을 제공한다. 이는 단위 프리미티브(unit primitive)를 순차적으로 조립하여 복잡한 대상을 구성하는 방식으로, 마치 LEGO 블록을 하나씩 쌓아 올리듯이 지식을 구성하는 원리다.

본 설계는 기존의 "보고서/제안서 논리적 모순 검출 및 수정 제안" 에이전트 기능에 **계층적 지식 구축(hierarchical knowledge construction)** 및 **brick-by-brick 개선 제안** 기능을 통합하는 방안을 제시한다.

---

## 2. 핵심 원칙: 트리 구조의 계층적 지식 체계

### 2.1 왜 트리(Tree)인가?

복합적인 지식 체계(system)는 자연스럽게 **트리 구조**를 형성한다:

```
[시스템(System)]
    ├── [하위시스템 A]
    │   ├── [개념 A1] (원자적)
    │   └── [개념 A2] (원자적)
    ├── [하위시스템 B]
    │   ├── [개념 B1] (원자적)
    │   └── [개념 B2] (원자적)
    └── [하위시스템 C]
        └── [개념 C1] (원자적)
```

- **하위(아래)**: 원자적 개념(atomic concept) — 더 이상 분해할 수 없는 기본 단위
- **상위(위)**: 복합적 체계 — 원자적 개념들이 조합되어 형성된 시스템

학습자는 **밑에서부터(bottom-up)** 원자적 개념을 이해하고, 이를 점차 조합하여 상위의 복합적 지식 체계를 쌓아 올린다.

### 2.2 OKF를 통한 트리 구조의 표현

OKF(Open Knowledge Format)는 이러한 계층적 지식 구조를 표현하기에 최적화되어 있다:

- 각 **개념(concept)** 은 하나의 Markdown 파일로 표현
- YAML frontmatter에 `type` 필드로 노드 유형 명시
- Markdown 링크로 개념 간 관계(특히 **계층적 관계**) 표현
- 구조적 Markdown(헤딩, 리스트, 테이블)을 활용해 **계층 정보**를 풍부하게 표현

OKF 번들은 그래프로 시각화할 수 있으며, 동일한 파일이 인간과 에이전트 모두에게 읽힐 수 있다.

---

## 3. 통합 아키텍처: OKF KB + ActiveGraph 기반 계층적 지식 에이전트

### 3.1 계층적 지식 구축을 위한 OKF KB 스키마 확장

기존 논리 검증 스키마에 **계층적 지식 구축**을 위한 노드/관계를 추가한다:

```yaml
# 추가 노드 타입
- AtomicConcept: 원자적 개념 (더 이상 분해 불가능한 기본 지식 단위)
- CompositeConcept: 복합 개념 (하위 개념들의 조합으로 형성된 체계)
- LearningPath: 학습 경로 (특정 시스템을 학습하기 위한 개념 순서)
- Prerequisite: 선행 조건 (특정 개념 이해에 필요한 선행 개념)

# 추가 관계 타입
- COMPOSED_OF: A가 B로 구성됨 (A는 Composite, B는 Atomic 또는 Composite)
- PREREQUISITE_OF: A가 B의 선행 조건임
- BUILDS_UPON: A가 B 위에 구축됨 (계층적 상승 관계)
- PART_OF_LEARNING_PATH: A가 특정 학습 경로의 일부임
- SUGGESTS_IMPROVEMENT: A가 B에 대한 개선안을 제안함
```

### 3.2 ActiveGraph 기반 에이전트 역할 정의

ActiveGraph는 **이벤트-소싱 반응형 그래프 런타임**으로, 모든 행동이 타입화된 이벤트 로그로 기록되고 그래프는 그 로그의 결정론적 투영(projection)이다. 이를 통해 **에이전트의 모든 학습 및 개선 활동이 완전히 감사 가능(auditable)** 해진다.

| 에이전트 역할 | 설명 | ActiveGraph 구현 |
|---|---|---|
| **Builder** | 원자적 개념을 이해하고 복합 개념을 조립 | `@llm_behavior` 패턴 구독으로 새로운 개념 추가 감지 |
| **Critic** | 논리적 모순 및 지식 격차 탐지 | 그래프 패턴 매칭으로 불완전한 계층 구조 탐지 |
| **Curator** | 지식 품질 관리 및 개선 제안 검토 | 인간 개발자(큐레이터)가 제안을 검토/승인 |
| **Improvement Agent** | 더 나은 brick-by-brick 구성을 제안 | 기존 트리 구조 분석 후 최적화 제안 생성 |

---

## 4. Brick-by-Brick 지식 구축 및 개선 프로세스

### 4.1 단계별 학습 프로세스

```
┌─────────────────────────────────────────────────────────────────────┐
│                    [학습 대상 시스템]                                │
│                         (CompositeConcept)                          │
└─────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │ COMPOSED_OF
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
        ┌───────────────────┐           ┌───────────────────┐
        │ [하위시스템 A]     │           │ [하위시스템 B]     │
        │ (CompositeConcept)│           │ (CompositeConcept)│
        └───────────────────┘           └───────────────────┘
                    ▲                               ▲
                    │ COMPOSED_OF                    │ COMPOSED_OF
        ┌───────────┴───────────┐       ┌───────────┴───────────┐
        ▼                       ▼       ▼                       ▼
┌───────────────┐       ┌───────────────┐       ┌───────────────┐
│ [개념 A1]     │       │ [개념 A2]     │       │ [개념 B1]     │
│(AtomicConcept)│       │(AtomicConcept)│       │(AtomicConcept)│
└───────────────┘       └───────────────┘       └───────────────┘
```

**Builder 에이전트의 학습 과정**:

1. **원자적 개념 식별**: 학습 자료에서 더 이상 분해할 수 없는 기본 개념을 추출
2. **선행 조건 확인**: 특정 개념을 이해하기 위해 필요한 선행 개념이 이미 학습되었는지 확인 (`PREREQUISITE_OF` 관계)
3. **하위 시스템 조립**: 원자적 개념들을 조합하여 하위 시스템(`CompositeConcept`) 구성
4. **시스템 완성**: 하위 시스템들을 조합하여 최종 시스템 구성

### 4.2 개선 제안(Improvement Proposal) 프로세스

**Improvement Agent**는 기존 트리 구조를 분석하고 더 나은 구성을 제안한다:

1. **현재 트리 구조 분석**
   - 각 노드의 연결성과 계층적 깊이 측정
   - 누락된 개념 또는 불필요하게 복잡한 경로 식별

2. **개선 방안 도출**
   - **재구성(Recomposition)**: 동일한 개념들을 더 효율적인 계층으로 재배치
   - **분할(Splitting)**: 과도하게 복잡한 개념을 더 작은 원자적 개념으로 분해
   - **병합(Merging)**: 지나치게 세분화된 개념들을 의미 있는 단위로 통합
   - **선행 조건 재정렬**: 더 논리적인 학습 순서 제안

3. **개선안 그래프에 반영**
   - `SUGGESTS_IMPROVEMENT` 관계로 기존 노드에 개선안 연결
   - 큐레이터의 검토 후 승인 시, 새로운 트리 구조로 대체

---

## 5. 통합 기능: 논리 검증 + 계층적 지식 구축

두 기능은 다음과 같이 **유기적으로 통합**된다:

### 5.1 논리 검증이 계층적 지식 구축을 지원

- **Critic 에이전트**가 계층 구조 내의 논리적 모순을 탐지:
  - 하위 개념들이 상위 주장을 실제로 지지하는지 검증
  - 선행 조건(`PREREQUISITE_OF`)이 논리적으로 타당한지 확인
  - 순환 의존성(cyclic dependency) 탐지

- **예시**: "개념 A가 개념 B 위에 구축되었지만(`BUILDS_UPON`), A의 선행 조건에 B가 포함되지 않은 경우" → 논리적 불일치로 보고

### 5.2 계층적 지식 구축이 논리 검증을 강화

- **Builder 에이전트**가 새로운 개념을 추가할 때, 자동으로 논리 검증 트리거:
  - 새로운 개념이 기존 주장과 모순되는지 확인
  - 새로운 개념이 기존 근거 체계와 일관되는지 검증

- **예시**: 새로운 원자적 개념이 추가될 때, Critic이 자동으로 활성화되어 기존 주장과의 모순 여부를 검사

### 5.3 통합 워크플로우

```
[새 학습 자료 입력]
        │
        ▼
[Builder: 원자적 개념 추출 및 노드 생성]
        │
        ▼
[Critic: 자동 활성화 - 기존 지식과의 논리적 일관성 검증]
        │
        ├─── [모순 발견] → [개선 제안 생성] → [큐레이터 검토]
        │
        ▼
[Builder: 계층적 조립 진행 (brick-by-brick)]
        │
        ▼
[Improvement Agent: 주기적 트리 구조 최적화 제안]
        │
        ▼
[큐레이터: 제안 검토 및 승인/거부]
        │
        ▼
[지식 그래프 업데이트 및 이벤트 로그 기록]
```

---

## 6. 구현 상세: ActiveGraph 기반 에이전트 구현

### 6.1 Builder 에이전트 구현 예시

```python
from activegraph import Graph, Runtime, llm_behavior

@llm_behavior(pattern="""
    MATCH (c:Concept)
    WHERE c.type = 'AtomicConcept' AND NOT exists((c)-[:COMPOSED_OF]->())
    RETURN c
""")
def builder_agent(graph, matched_nodes):
    """
    원자적 개념들을 감지하고, 이들을 조합하여 복합 개념을 구성.
    """
    for concept in matched_nodes:
        # 1. 이 개념의 선행 조건 확인
        prerequisites = graph.query("""
            MATCH (p:Concept)-[:PREREQUISITE_OF]->(c)
            WHERE c.id = $concept_id
            RETURN p
        """, concept_id=concept.id)
        
        # 2. 선행 조건이 모두 충족되었는지 확인
        if all(p.learned for p in prerequisites):
            # 3. 상위 복합 개념 조립 시도
            composite = assemble_composite(concept, prerequisites)
            if composite:
                graph.add_node(composite, type='CompositeConcept')
                graph.add_edge(composite, concept, 'COMPOSED_OF')
                for p in prerequisites:
                    graph.add_edge(composite, p, 'COMPOSED_OF')
```

### 6.2 Improvement Agent 구현 예시

```python
@llm_behavior(pattern="""
    MATCH (s:System)-[:COMPOSED_OF*]->(c:Concept)
    RETURN s, collect(c) as components
""")
def improvement_agent(graph, matched_systems):
    """
    시스템의 트리 구조를 분석하고 개선안을 제안.
    """
    for system in matched_systems:
        components = system['components']
        
        # 1. 트리 깊이 및 균형 분석
        depth_analysis = analyze_tree_depth(components)
        
        # 2. 개선 제안 생성 (LLM 기반)
        improvement = generate_improvement(
            current_structure=components,
            depth_analysis=depth_analysis
        )
        
        # 3. 개선안을 그래프에 연결
        if improvement:
            graph.add_node(improvement, type='ImprovementSuggestion')
            graph.add_edge(system, improvement, 'SUGGESTS_IMPROVEMENT')
```

---

## 7. 큐레이터(개발자)의 역할

이 시스템에서 개발자(큐레이터)의 역할은 다음과 같이 재정의된다:

| 기존 역할 | 새로운 큐레이터 역할 |
|---|---|
| 모든 로직 직접 코딩 | **온톨로지와 규칙 정의** — 어떤 개념이 원자적인지, 어떤 관계가 유효한지 정의 |
| 모든 예외 케이스 처리 | **에이전트 제안 검토 및 승인** — 에이전트가 제안한 개선안을 검토하고 승인/거부 |
| 시스템 아키텍처 설계 | **지식 그래프 품질 관리** — OKF KB의 정확성, 완전성, 일관성 유지 |
| 디버깅 및 오류 수정 | **학습 경로(curriculum) 설계** — 어떤 순서로 개념을 학습할지 정의 |

큐레이터는 더 이상 코드를 작성하는 대신, **에이전트가 학습하고 추론할 수 있는 지식 기반(OKF KB)과 실행 환경(ActiveGraph)을 조성**하는 역할에 집중한다.

---

## 8. 기대 효과

1. **Understanding 병목 해소**: 에이전트가 원자적 개념부터 체계적으로 학습을 안내하므로, 학습자의 인지 부하가 감소한다.

2. **지식의 유기적 성장**: OKF KB 기반으로 지식이 지속적으로 확장되고 개선되며, 모든 변경 사항이 ActiveGraph 이벤트 로그에 기록되어 추적 가능하다.

3. **이중적 가치 창출**:
   - **문서 품질 향상**: 논리적 모순 검출 및 수정 제안
   - **학습 효율성 향상**: 계층적 지식 구축 및 개선 제안

4. **완전한 감사 가능성**: 모든 에이전트 행동이 이벤트 로그로 기록되어, 어떤 결정이 어떻게 내려졌는지 완전히 추적 가능하다.

5. **개발자의 전략적 역할 전환**: 개발자는 코드 작성자에서 **지식 큐레이터**로 역할이 전환되어, 더 높은 수준의 설계와 품질 관리에 집중할 수 있다.

---

## 9. 결론

> "아키텍처는 설계되는 것이 아니라 도출되어야 한다."

이 원칙은 계층적 지식 구축 시스템에도 동일하게 적용된다. OKF KB와 ActiveGraph의 결합은 **에이전트가 실제로 학습하고 추론하는 과정을 관찰**함으로써, 최적의 지식 트리 구조가 자연스럽게 도출되는 환경을 제공한다.

**Brick-by-brick** 접근법은 단순한 지식 축적이 아닌, **이해(understanding)를 중심으로 한 진정한 학습**을 가능하게 한다. 그리고 **Improvement Agent**는 이렇게 구축된 지식 체계가 더 나은 방향으로 지속적으로 진화하도록 돕는다.

이 설계는 보고서/제안서의 논리 검증이라는 실용적 니즈와, AI 시대의 근본적 과제인 **"이해의 병목 해소"** 를 동시에 해결하는 통합적 접근법이다. OKF KB는 **정적 지식의 그래프**로, ActiveGraph는 **동적 학습과 개선의 그래프**로 기능하며, 이 두 그래프의 유기적 상호작용을 통해 **살아있는 지식 생태계**가 구현된다.